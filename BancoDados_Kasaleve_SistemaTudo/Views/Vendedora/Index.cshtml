@model IEnumerable<BancoDados_Kasaleve_SistemaTudo.Models.Vendedora>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.vendedoraNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.vendedoraNome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.vendedoraId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.vendedoraId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.vendedoraId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
